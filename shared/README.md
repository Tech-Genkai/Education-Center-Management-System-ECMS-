# Shared Package

Place DTOs, constants, and TypeScript types that are consumed by both backend and frontend.

- `dto/`: request/response DTOs
- `constants/`: shared enums and string literals
- `typings/`: TypeScript types/interfaces
